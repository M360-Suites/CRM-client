import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Calendar | CRM360",
};

const Page = () => {
  return <div className="bg-yellow-400">This is the Calendar</div>;
};

export default Page;
